apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  nginx.conf: |
    server {
      listen 80;
      server_name sausage-backend;

      location / {
        proxy_pass http://backend-service:8080;
      }
    }
